## [Unreleased]

- Add `images` for logos, watermarks, and other artwork Proofsheet did not capture, read from a local path, downloaded from a URL, or downloaded from an `<img>` selected on any webpage
- Draw them in compositions with `image:` layers, which take the same crop, resize, radius, rotation, and shadow options as `shot:` layers

## [0.2.0] - 2026-09-19

- Add the `compose` command for arranging captured screenshots on solid, transparent, gradient, or image backgrounds
- Add per-layer cropping, proportional resizing, rounded corners, rotation, and shadows

## [0.1.0] - 2026-09-09

- Initial release
